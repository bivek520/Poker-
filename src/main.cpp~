#include <iostream>
#include "player.h"
#include "dealer.h"
#include "MainWindow.h"

int main (int argc, char *argv[]) {
    auto app = Gtk::Application::create(argc, argv, APP_NAME);
    MainWindow win;
    return app->run(win);
}
